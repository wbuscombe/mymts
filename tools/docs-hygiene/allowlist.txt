# docs-hygiene allowlist — GLOBAL exemptions (literal matched strings that are
# OK wherever they appear in the public docs). One entry per line, `# reason`.
# A NEW non-allowlisted match still fails the build. For one-off legit cases
# (e.g. a CHANGELOG entry that quotes a removed phrase to document a cleanup),
# use the inline marker `docs-hygiene:allow` on that line instead of adding the
# literal here (so the string stays caught everywhere else).

# --- generic / non-routable IPs (not topology) ---
10.0.2.2     # Android emulator → host loopback alias (the demo TV-app helper URL)
127.0.0.1    # localhost (health checks, in-container self-fetch)
0.0.0.0      # bind-all listen address (uvicorn/compose)

# --- deliberately-kept last-octet box aliases ---
# Non-routable last-octet shorthand, NOT a full IP. Load-bearing in the AGENTS.md
# governance rules ("never adb kill-server — it disrupts the other boxes .182/.158")
# and pervasive in the CHANGELOG history. The prior topology scrub kept these by
# decision; the personal-config audit flagged them as the operator's call.
.92          # the MyMTS box alias
.158         # an other-Onn-box alias
.182         # the WyzeGrid/camera box alias
