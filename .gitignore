# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Editor / IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.swp
*.swo

# Android / Gradle
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
*.apk
*.aab
*.ap_
*.dex
*.class
release/
debug/
proguard/
*.keystore
*.jks
keystore.properties

# Helper TLS material — private keys NEVER in repo. The public cert
# *is* in app/src/main/res/raw/helper_cert.pem because the app trusts
# it as a pinned trust anchor; only the private key (helper-side) is
# secret. Belt-and-braces: exclude any helper.key / *.pem file outside
# the app trust path.
*.key
helper/**/*.crt
helper/**/*.pem
!app/src/main/res/raw/helper_cert.pem

# Java / Kotlin
*.log
hs_err_pid*

# Node (helper toolchain, just in case)
node_modules/
dist/
.npm/

# Demo recordings (tools/capture) — large video binaries, never committed.
tools/capture/output/

# Python (helper)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local env / secrets — placeholder; real .env never enters the repo
.env
.env.*
!.env.example
*.local

# Local archive — keeps source zips and reference material outside the repo tree
.archive/

# Build/release artifacts
*.zip
*.tar
*.tar.gz
*.tgz

# Coverage / test artifacts
coverage/
*.lcov
htmlcov/
.coverage

# Soak-run raw event captures. These can grow tens-to-hundreds of MB per
# run and are regenerable from the harness. The committed per-run record is
# the small files (meta.json, end.json, runner.log, meminfo.csv, device.txt)
# plus the parsed summary in the finding doc.
docs/findings/runs/**/events.log

# Re-include runner.log inside run directories — it's small, captures the
# host-side timeline, and the broader *.log rule above would otherwise
# exclude it.
!docs/findings/runs/**/runner.log

# Operator-local operational runbooks (deploy/provisioning/rotation/recovery).
# Environment-specific: host names, IPs, paths, device inventory live ONLY here.
docs/ops-local/

# docs-hygiene: the operator's real apex/brand token (template is apex.local.example).
tools/docs-hygiene/apex.local

# Operator-local deploy targets (real host/IP/path); template is .example.
scripts/deploy.local.env
# Defensive: any *.local.env anywhere is operator-local, never committed.
*.local.env
**/*.local.env

# Local-only working artifact (perf telemetry) — kept on disk, never committed
docs/perf-analysis-2026-06.md

# Review/analysis/audit REPORTS are operator deliverables, NOT repo artifacts —
# never commit them (research that feeds build decisions lives in docs/findings/,
# BACKLOG.md, ARCHITECTURE.md, etc., which ARE tracked). Narrow to the review-report
# filename pattern only.
docs/adversarial-review-*.md

# Desktop packaging (PyInstaller) — built binaries + build artifacts, NEVER committed.
# Only the tooling (spec/build script/launcher/docs) is tracked.
tools/desktop/build/
tools/desktop/dist/
*.pyinstaller.spec.bak

# Per-deployment lineup override (operator data; the .example is committed)
lineup.local.json
**/lineup.local.json

# Server-side wall config (headless-container version; runtime operator state,
# lives in the data dir — seedless, never committed)
wall.local.json
**/wall.local.json
